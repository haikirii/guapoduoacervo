export interface CardFanartProps {
  linkFanart: string;
  img: string;
  alt?: string;
  title: string;
  artist: string;
  profPic: string;
  isHovered?: boolean; // Mantendo como obrigatório
}